#include <iostream>
#include <string>

using namespace std;

int main()
{
    string left;
    getline(cin, left);
    string right;
    getline(cin, right);
    cout << right << endl << left << endl;
    return 0;
}